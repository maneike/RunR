import React from 'react';
import {SafeAreaView} from 'react-native';
import MainScreen from './src/MainScreen';

const App: React.FC = () => {
  return (
    <SafeAreaView>
      <MainScreen />
    </SafeAreaView>
  );
};
export default App;
